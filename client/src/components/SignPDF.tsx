import React from 'react';

const SignPDF: React.FC = () => {
    return (
        <div>
            <h1>Sign Your PDF</h1>
            <button>Sign PDF</button>
        </div>
    );
};

export default SignPDF;
